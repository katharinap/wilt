<!DOCTYPE html>
<html lang="en">
    <head>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<meta name="description" content="">
	<meta name="author" content="">

	<title>What I Learned Today</title>
	<link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
    </head>

    <body style="background-image: url(<%= static_path(@conn, "/images/keyboard_1920.jpg") %>); background-repeat: no-repeat; background-attachment: fixed">
	<div class="container-fluid">
	    <header class="header">
		<nav role="navigation">
		    <ul class="nav nav-pills pull-right">
 			<%= if logged_in?(@conn) do %>
			    <li class="nav-item"><%= String.upcase(current_user(@conn).username) %></li>
			    <li class="nav-item"><%= link to: user_path(@conn, :edit, current_user(@conn)), class: "icon-link" do %>
				<i class="fa fa-cog fa-lg" aria-muted="true"></i>
			    <% end %>
			    </li>
			    <li class="nav-item"><%= link to: session_path(@conn, :delete), method: :delete, class: "icon-link" do %>
				<i class="fa fa-power-off fa-lg" aria-muted="true"></i>
			    <% end %>
			    </li>
			<% else %>
			    <li class="nav-item"><%= link "Login", to: session_path(@conn, :new) %></li>
			<% end %>
		    </ul>
		</nav>
		<div class="page-title">
		    What I Learned Today
		</div>
	    </header>

	    <%= if get_flash(@conn, :info) do %>
		<p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
	    <% end %>
	    <%= if get_flash(@conn, :error) do %>
		<p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
	    <% end %>
	    <main role="main">
		<%= render @view_module, @view_template, assigns %>
	    </main>

	</div> <!-- /container -->
	<script src="<%= static_path(@conn, "/js/app.js") %>"></script>
	<script>require("js/app").App.run()</script>
    </body>
</html>

